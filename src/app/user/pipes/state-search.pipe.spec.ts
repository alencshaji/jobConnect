import { StateSearchPipe } from './state-search.pipe';

describe('StateSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StateSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
