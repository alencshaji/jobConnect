import { HeaderSearchPipe } from './header-search.pipe';

describe('HeaderSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
